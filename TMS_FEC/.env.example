# Backend API base URL (no trailing slash on /api)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Optional WebSocket URL (defaults to API host without /api)
# NEXT_PUBLIC_WS_URL=http://localhost:8000
